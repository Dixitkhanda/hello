# Add this in your script inside `run: |`
R=$(( RANDOM % 2 ))
if [ $R -eq 1 ]; then
  echo "// Smart Random $(date)" >> hello.cpp
  git config --global user.name "Dixit Kumar"
  git config --global user.email "dixitkhanda1@gmail.com"
  git add hello.cpp
  git commit -m "Smart commit on $(date)"
  git push
else
  echo "Skipped today's commit, randomness active"
fi
